-- ID топика в форум-супергруппе мастера. Если чат мастера — форум,
-- бот создаёт отдельный топик на каждую заявку и шлёт все сообщения
-- по ней (уведомление, голос, фото, переписку, смены статуса) с
-- message_thread_id = topic_id. Каждая заявка визуально живёт в своём
-- окне, и 7-8 параллельных клиентов не сваливаются в одну ленту.
ALTER TABLE orders ADD COLUMN topic_id INTEGER;
